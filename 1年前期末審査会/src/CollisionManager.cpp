#include "CollisionManager.h"
#include "Collision.h"
#include "scene\Stage.h"
#include "object\Object.h"

CollisionManager::CollisionManager(){
}


CollisionManager::~CollisionManager(){
}

void CollisionManager::Update(){
	//if (CStage::m_player->GetState() == Object::State::LIVE){
	//	for (auto &Enemy : CStage::m_enemy){
	//		if (Enemy->GetState() == Object::State::LIVE){
	//			if (CCollision::isCollision(CStage::m_player->GetPos(), Enemy->GetPos(), CStage::m_player->GetR(), Enemy->GetR()));
	//			CStage::m_player->isHit();
	//		}
	//	}
	//}
}